type Query {
    login(loginInput: LoginInput!): LoginOutput!
}

type Mutation {
    register(user: addUserDto!): User
}

input LoginInput {
    username: String!
    password: String!
}

type LoginOutput {
    jwt: String
}